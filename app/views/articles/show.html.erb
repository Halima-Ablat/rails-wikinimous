<div class="container my-6 col-6 text-center">
<br/>
<br/>
<br/>
<h2><%= @article.title %>
  <small>
  <%= link_to edit_article_path(@article) do %><i class="fa-solid fa-pen"></i><% end %>
  <%= link_to article_path(@article),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} do %><i class="fa-solid fa-trash-can"></i><% end %>
  </small>
</h2>
<p class="my-4"><%= @article.content %></p>
<%= link_to "Back", articles_path %>
</div>
